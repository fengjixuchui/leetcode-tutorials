## Backtracking

```go
func bt (result *[][]int) {
    // 終止條件判斷
    if {
        // 并加入結果
		*ans = append(*ans, append([]int{}, elem))
		return
    }

    // 回溯
    for  {
        // 修改

        // 往下遞歸
        bt()
        // 還原
    }
}

func function() {
    result := make([][]int, 0)
	bt(&result)
	return result
}
```
